"use client";

import React from 'react'
import { AuthProvider } from './contexts/AuthContext';

export function Providers({ children, modal }: { children: React.ReactNode, modal: React.ReactNode; }) {
  return (
    // <AuthProvider>{children} {modal}</AuthProvider>
    <></>
  );
}
